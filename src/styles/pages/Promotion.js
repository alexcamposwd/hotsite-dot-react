import styled from "styled-components";

export const PromotionContainer = styled.article`
  width: 100vw;
  min-width: 360px;
  height: auto;
  overflow: hidden;
  background-color: ${(props) => props.theme.colors.grey3};
`;
